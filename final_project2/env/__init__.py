"""Portfolio environment (MDP simulator).

Populated in **Milestone 2**. Exposes :class:`PortfolioEnv`, a gym-style
reset/step interface consuming a precomputed feature matrix + return matrix.
Tests under ``tests/test_env.py`` are written before the implementation.
"""
