"""Market-data ingestion and offline-dataset construction.

Populated in **Milestone 1** (``download_data.py``) and **Milestone 3**
(``dataset_builder.py``). Raw price files are cached under
``cfg.data.cache_dir``; offline transition datasets live under ``datasets/``.
"""
